//%attributes = {}
#DECLARE($traceVisible : Boolean; $allPeopleVisible : Boolean; $currentPeopleVisible : Boolean)

OBJECT SET VISIBLE:C603(*; "VPPeople"; $allPeopleVisible)

OBJECT SET VISIBLE:C603(*; "VPCurrentPeople"; $currentPeopleVisible)

OBJECT SET VISIBLE:C603(*; "trace"; $traceVisible)
